# Personal data (user fills these)
cv.md
data/applications.md
data/pipeline.md
data/scan-history.tsv
data/follow-ups.md
data/parser-output/**/*.json
!data/parser-output/.gitkeep
!data/parser-output/**/.gitkeep
reports/*.md
!reports/.gitkeep
output/*
!output/.gitkeep
batch/logs/*
!batch/logs/.gitkeep
batch/batch-state.tsv
batch/batch-input.tsv
batch/tracker-additions/**/*.tsv
!batch/tracker-additions/.gitkeep
jds/*
!jds/.gitkeep

# Writing samples (user's personal files — never committed)
writing-samples/*
!writing-samples/README.md

# User config and customization (never auto-updated)
config/profile.yml
portals.yml
modes/_profile.md
.update-dismissed
.update-lock

# Secrets (never commit — use .env.example as template)
.env

# Generated
.resolved-prompt-*
node_modules/
bun.lock

# OS
.DS_Store
*.mov
*.mp4

# Claude Code local
.claude/settings.local.json
.claude/memory/
.claude/last-sync.txt
.claude/hooks/check-sync.mjs
career-dashboard

# Debug / temp (parser development artifacts)
data/dumps/
data/*.backup
data/*-stderr.txt
data/*-check.json
data/*-stdout.txt

# ── Outputs de sesión / temporales ──────────────────────
*.png
*.pdf
scan-*.log
scan-output.txt
fff.html
project-context.txt

# ── Dumps y parser output ────────────────────────────────
dumps/
data/parser-output/*/

# ── Backups ──────────────────────────────────────────────
backups/
*.bak

# ── Scripts ad-hoc / one-off ─────────────────────────────
_apply_nunez_navarro.mjs
_turijobs_search.mjs
_extract_novotel.mjs
dump-minor.mjs
test-accor-raw.mjs
bundle-project.py
*.backup
profiles/*/conversacion-inicial*.txt
